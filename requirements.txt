# 核心依赖
PyMuPDF>=1.23.0
python-docx>=1.1.0
Pillow>=10.0.0
openpyxl>=3.1.0

# PDF 导出
reportlab>=4.0.0

# CAD 支持（可选）
ezdxf>=1.2.0
matplotlib>=3.7.0

# Windows COM/窗口嵌入（DWG查看器需要）
pywin32>=306

# 打包
pyinstaller>=6.0.0